# 设计验收

final result: passed

## 对照依据

- 选定设计：`design/selected-reference.png`，用户选择的第 1 张图，1160 × 1356 像素。
- 最终程序：`dist/LLG订阅助手.exe`，原生 WPF 窗口，默认 500 × 580 DIP，最小 480 × 570 DIP。
- 实际截图：`qa/screenshots/final-updated.png`，486 × 573 像素。窗口捕获裁掉部分系统边框，截图尺寸与 WPF 外框 DIP 不直接等同；该程序没有 CSS 视口。
- 对照图：`qa/screenshots/comparison-final.png`，996 × 573 像素。左侧设计按实际截图宽度等比缩小至 486 × 568，右侧保留原始运行截图；未拉伸或修改运行画面。
- 状态：均为成功更新 44 个节点、两个主要操作可用。设计中的 19:20 是示例，实际截图 20:21 是真实更新时间。
- 指针蓝色光晕属于自动化捕获覆盖层，原生标题栏由 Windows 提供；这两项不作为应用内容差异。

## 对照历史

1. 初版：`qa/screenshots/main-updated.png`。默认高度 610 DIP，使操作区与状态区留白偏大，记为 P2。
2. 修正：将默认窗口高度收紧为 580 DIP，最小高度设为 570 DIP，保留按钮高度与中文字号。
3. 复验：打开最终发布 EXE，读取已保存的真实列表，重新截图并与源图在同一张对照图中检查。上述留白问题已解决，无待处理 P0/P1/P2。

## 必须检查的五个方面

- 字体：使用 Microsoft YaHei UI / Segoe UI；保留粗体标题、22 DIP 按钮文字和 13–16 DIP 辅助文字。所有中文、按钮和提示均完整可读。原生字体抗锯齿与生成图略有差别，属于 P3。
- 布局：右上角设置、两枚上下排列的大按钮、底部两行状态、分隔线与粘贴指引，顺序与层级一致。全宽按钮保持 82 DIP 高度和 16 DIP 间隔，没有内容溢出或被遮住。
- 颜色：蓝色主按钮、浅蓝次按钮、近白背景、深色正文与灰色辅助文字保持一致；错误提示另用红色，加载中按钮置灰。
- 资产：窗口图标来自所选设计生成的独立 PNG/ICO；成功、时钟、信息和错误图标使用微软 Fluent System Icons 的原始矢量路径。没有用临时文字或手画图形替代图标。
- 文案：三个操作名称与用户要求完全一致。复制完成明确提示在 v2rayN 主窗口按 Ctrl+V，成功数量来自实际解析结果。

完整对照图已足以读清所有内容；设置弹窗另外查看了独立截图 `qa/screenshots/settings-empty.png`，无需对主界面再裁切局部区域。

## 交互检查

- 独立 EXE 在只放一个 EXE 的测试目录启动；子进程的 DOTNET_ROOT 指向不存在的目录，PATH 不含 .NET，仍可正常显示。
- 空数据启动时提示先保存账号，转换按钮不可用。
- 无账号时点击更新打开设置；用户名、密码输入框及保存按钮完整显示。
- 空内容保存显示必填提示；关闭设置后返回主界面。
- 已保存账号下实际更新成功：44 个节点；加载时禁用重复操作，完成后恢复。
- 点击转换后，实际剪贴板内容与这 44 条分享链接逐字一致。
- 最终版重新启动后读取加密保存的账号与 44 个节点，转换按钮可用。
- 本机 v2rayN 数据库只读核对发现 44 个匹配的第一方节点，连接身份、SNI 与证书验证设置一致。

## 验收范围

已完成上述当前 Windows 会话的原生窗口验证。没有宣称覆盖所有 Windows/DPI 组合、节点连通性或未出现的协议格式。没有修改 v2rayN 数据库、选择节点或切换系统代理。

后续仅有原生标题栏和字体栅格化的 P3 差异，不阻塞交付。
